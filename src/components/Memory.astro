<section class="memory-section" id="memory" aria-labelledby="memory-title">
  <div class="container container--wide memory-grid">
    <div class="memory-art reveal" aria-hidden="true">
      <img src="/social/media/memory.svg" alt="" />
    </div>
    <div class="memory-copy">
      <p class="section-label reveal">Experimental local memory</p>
      <h2 id="memory-title" class="reveal reveal-delay-1">Memory should be read, not injected.</h2>
      <p class="memory-lead reveal reveal-delay-2">
        ppmlx can capture scoped events in a local temporal graph. It does not add hidden memory to every prompt.
      </p>
      <div class="memory-boundary reveal reveal-delay-3">
        <div>
          <span>Available</span>
          <strong>Local capture, search, graph tools, and MCP server</strong>
        </div>
        <div>
          <span>Read grant</span>
          <strong>A user-added read skill with explicit tools and project scope</strong>
        </div>
      </div>
      <p class="memory-note reveal">The harness decides when memory can help. ppmlx enforces the read grant.</p>
    </div>
  </div>
</section>

<style>
  .memory-section { position: relative; z-index: 1; overflow: hidden; padding: var(--section-gap) 0; }
  .memory-grid {
    display: grid;
    grid-template-columns: minmax(300px, 0.72fr) minmax(0, 1.28fr);
    gap: clamp(48px, 8vw, 108px);
    align-items: center;
  }
  .memory-art { position: relative; display: grid; min-height: 440px; place-items: center; }
  .memory-art::before {
    position: absolute;
    width: 78%;
    aspect-ratio: 1;
    border-radius: 50%;
    content: '';
    background: radial-gradient(circle, rgba(212, 131, 60, 0.12), transparent 68%);
    animation: memory-glow 6s ease-in-out infinite;
  }
  .memory-art img {
    position: relative;
    display: block;
    width: min(430px, 118%);
    max-height: 500px;
    object-fit: contain;
    filter: invert(55%) sepia(45%) saturate(1060%) hue-rotate(344deg) brightness(91%) contrast(88%);
    animation: memory-float 7s ease-in-out infinite;
  }
  .memory-copy h2 {
    max-width: 700px;
    margin-bottom: 22px;
    font-family: var(--font-display);
    font-size: clamp(36px, 4.7vw, 58px);
    font-weight: 300;
    letter-spacing: -0.03em;
    line-height: 1.02;
  }
  .memory-lead { max-width: 680px; color: var(--text-secondary); font-size: 18px; line-height: 1.65; }
  .memory-boundary { display: grid; grid-template-columns: 1fr 1fr; gap: 1px; margin-top: 32px; border: 1px solid var(--border); background: var(--border); }
  .memory-boundary > div { padding: 22px; background: var(--bg-surface); transition: background 0.25s ease, transform 0.25s ease; }
  .memory-boundary > div:hover { z-index: 1; background: var(--bg-elevated); transform: translateY(-3px); }
  .memory-boundary span { display: block; margin-bottom: 9px; color: var(--teal); font-family: var(--font-mono); font-size: 10px; text-transform: uppercase; }
  .memory-boundary > div:last-child span { color: var(--accent); }
  .memory-boundary strong { display: block; color: var(--text-secondary); font-size: 13px; font-weight: 400; line-height: 1.55; }
  .memory-note { margin-top: 18px; color: var(--text-dim); font-family: var(--font-mono); font-size: 11px; }

  @keyframes memory-float { 50% { transform: translateY(-12px) rotate(0.5deg); } }
  @keyframes memory-glow { 50% { opacity: 0.62; transform: scale(1.08); } }

  @media (max-width: 820px) {
    .memory-grid { grid-template-columns: 1fr; }
    .memory-art { order: 2; min-height: 340px; }
    .memory-art img { max-height: 390px; }
  }
  @media (max-width: 540px) {
    .memory-boundary { grid-template-columns: 1fr; }
  }
  @media (prefers-reduced-motion: reduce) {
    .memory-art::before, .memory-art img { animation: none; }
    .memory-boundary > div:hover { transform: none; }
  }
</style>
