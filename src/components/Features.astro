<section id="routes" class="container feature-section" aria-label="Routing features">
  <div class="feature-grid">
    <div class="feature-intro">
      <h2>A small layer.<br />More choice.</h2>
    </div>
    <div class="feature-list">
      <article>
        <h3>Routes you control</h3>
        <p>Give each app a custom model name. Map it to a configured provider and model. Change the route when your needs change.</p>
        <a class="quiet-link" href="https://github.com/wydrox/ppmlx#connect-your-tools">Connect your tools</a>
      </article>
      <article>
        <h3>A policy for every request</h3>
        <p>Put a supported subscription or lower-cost model first. Use fallback rules when a provider is unavailable.</p>
        <a class="quiet-link" href="https://github.com/wydrox/ppmlx/blob/main/docs/architecture/adr/0005-routing-and-fallback.md">Explore route policies</a>
      </article>
      <article>
        <h3>Local from the start</h3>
        <p>Run MLX models directly on Apple Silicon. Keep a local route for private work.</p>
        <a class="quiet-link" href="https://github.com/wydrox/ppmlx#quick-start">Run a local model</a>
      </article>
    </div>
  </div>
</section>
<style>
.feature-section { padding-block: clamp(72px, 9vw, 128px); }
.feature-grid { display: grid; grid-template-columns: minmax(0, 34%) minmax(0, 1fr); column-gap: 72px; }
.feature-intro h2 { font-size: 32px; line-height: 1.1; margin: 0; }
.feature-list { display: grid; gap: 42px; }
.feature-list h3 { font-size: 19px; line-height: 1.3; margin: 0 0 10px; }
.feature-list p { font-size: 15px; line-height: 1.8; max-width: 440px; margin: 0 0 13px; }
.quiet-link { font: 13px/1.7 var(--font-mono); }
@media (max-width: 700px) { .feature-grid { grid-template-columns: 1fr; gap: 32px; } }
</style>
