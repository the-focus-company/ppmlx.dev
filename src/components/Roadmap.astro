<section class="direction" id="direction" aria-labelledby="direction-title">
  <div class="container container--wide">
    <div class="direction-head reveal">
      <div>
        <p class="section-label">The switchboard</p>
        <h2 id="direction-title">One switchboard under every coding agent.</h2>
      </div>
      <p>
        A harness keeps its native protocol. ppmlx selects an approved model, preserves tool meaning, and returns the expected response shape.
      </p>
    </div>

    <div class="direction-flow reveal reveal-delay-1" aria-label="ppmlx request flow">
      <article>
        <span class="step">Input</span>
        <h3>Keep the harness</h3>
        <p>Claude Code, Codex, OpenCode, and Pi keep their own request format.</p>
        <code>Messages · Responses · Chat</code>
      </article>
      <div class="flow-arrow" aria-hidden="true"><i></i><span>&rarr;</span></div>
      <article class="core">
        <span class="step">ppmlx</span>
        <h3>Normalize and route</h3>
        <p>The policy checks tools, privacy, health, account, and fallback rules.</p>
        <code>Agent IR · route policy</code>
      </article>
      <div class="flow-arrow" aria-hidden="true"><i></i><span>&rarr;</span></div>
      <article>
        <span class="step">Output</span>
        <h3>Use the selected model</h3>
        <p>A local MLX model or an approved provider answers the request.</p>
        <code>local first · remote opt-in</code>
      </article>
    </div>

    <div class="contract-strip reveal reveal-delay-2">
      <div>
        <span class="contract-state"><i></i> Contract accepted</span>
        <strong>Runtime support follows exact replay tests.</strong>
      </div>
      <p>No silent tool repair. No credential reuse from another app. No fallback after output starts.</p>
      <a href="https://github.com/wydrox/ppmlx/tree/main/docs/architecture" target="_blank" rel="noopener">Read the decisions &rarr;</a>
    </div>
  </div>
</section>

<style>
  .direction {
    position: relative;
    z-index: 1;
    padding: var(--section-gap) 0;
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    background: linear-gradient(180deg, rgba(13, 15, 19, 0.82), rgba(7, 8, 10, 0.72));
  }
  .direction-head {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(300px, 0.72fr);
    gap: 64px;
    align-items: end;
    margin-bottom: 48px;
  }
  .direction-head h2 {
    max-width: 720px;
    font-family: var(--font-display);
    font-size: clamp(34px, 4.8vw, 58px);
    font-weight: 300;
    letter-spacing: -0.03em;
    line-height: 1.04;
  }
  .direction-head > p { color: var(--text-secondary); font-size: 17px; line-height: 1.65; }
  .direction-flow { display: grid; grid-template-columns: 1fr 64px 1fr 64px 1fr; align-items: stretch; }
  .direction-flow article {
    min-height: 250px;
    padding: clamp(24px, 3vw, 34px);
    border: 1px solid var(--border);
    border-radius: 10px;
    background: var(--bg-surface);
    transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1), border-color 0.3s ease, background 0.3s ease, box-shadow 0.3s ease;
  }
  .direction-flow article:hover { z-index: 1; border-color: #353b46; background: var(--bg-elevated); box-shadow: 0 20px 54px rgba(0, 0, 0, 0.26); transform: translateY(-5px); }
  .direction-flow article.core {
    border-color: rgba(212, 131, 60, 0.55);
    box-shadow: inset 0 0 0 1px rgba(212, 131, 60, 0.08), 0 18px 54px rgba(0, 0, 0, 0.22);
    animation: core-breathe 5s ease-in-out infinite;
  }
  .step {
    color: var(--accent);
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.09em;
    text-transform: uppercase;
  }
  .direction-flow h3 { margin: 42px 0 12px; font-size: 19px; font-weight: 400; }
  .direction-flow p { min-height: 72px; color: var(--text-secondary); font-size: 14px; line-height: 1.65; }
  .direction-flow code { display: block; margin-top: 22px; color: var(--text-dim); font-family: var(--font-mono); font-size: 10px; }
  .flow-arrow { display: flex; align-items: center; color: var(--accent); }
  .flow-arrow i { width: 100%; height: 1px; background: linear-gradient(90deg, var(--border), var(--accent), var(--border)); background-size: 200% 100%; animation: flow-line 2.4s linear infinite; }
  .flow-arrow span { margin-left: -3px; font-family: var(--font-mono); font-size: 15px; animation: arrow-nudge 1.4s ease-in-out infinite; }
  .contract-strip {
    display: grid;
    grid-template-columns: 0.8fr 1.3fr auto;
    gap: 36px;
    align-items: center;
    margin-top: 24px;
    padding: 22px 24px;
    border: 1px solid var(--border);
    border-radius: 10px;
    background: rgba(13, 15, 19, 0.72);
  }
  .contract-strip > div { display: flex; flex-direction: column; gap: 5px; }
  .contract-state { display: inline-flex; align-items: center; gap: 7px; color: var(--teal); font-family: var(--font-mono); font-size: 10px; text-transform: uppercase; }
  .contract-state i { width: 6px; height: 6px; border-radius: 50%; background: var(--teal); }
  .contract-strip strong { font-size: 13px; font-weight: 400; }
  .contract-strip p { color: var(--text-dim); font-size: 12px; }
  .contract-strip a { color: var(--accent); font-family: var(--font-mono); font-size: 11px; text-decoration: none; white-space: nowrap; }
  .contract-strip a:hover { color: var(--accent-bright); }

  @keyframes flow-line { to { background-position: -200% 0; } }
  @keyframes arrow-nudge { 50% { transform: translateX(4px); } }
  @keyframes core-breathe {
    0%, 100% { box-shadow: inset 0 0 0 1px rgba(212, 131, 60, 0.08), 0 18px 54px rgba(0, 0, 0, 0.22); }
    50% { box-shadow: inset 0 0 0 1px rgba(212, 131, 60, 0.14), 0 18px 64px rgba(212, 131, 60, 0.08); }
  }

  @media (max-width: 960px) {
    .direction-head { grid-template-columns: 1fr; gap: 20px; }
    .direction-flow { grid-template-columns: 1fr; gap: 0; }
    .flow-arrow { flex-direction: column; height: 46px; justify-content: center; }
    .flow-arrow i { width: 1px; height: 100%; background: linear-gradient(180deg, var(--border), var(--accent)); }
    .flow-arrow span { display: none; }
    .direction-flow article { min-height: auto; }
    .direction-flow h3 { margin-top: 24px; }
    .direction-flow p { min-height: auto; }
    .contract-strip { grid-template-columns: 1fr; gap: 14px; }
  }
  @media (prefers-reduced-motion: reduce) {
    .direction-flow article.core, .flow-arrow i, .flow-arrow span { animation: none; }
    .direction-flow article:hover { transform: none; }
  }
</style>
