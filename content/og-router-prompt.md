# Social preview artwork

This generated concept was replaced by `og-source.svg` and `public/og-brand.png` in version 0.11.3.

Generated with the built-in image generation tool on 2026-09-07.
Asset: `public/og-router.png`, exported at 1200 × 630 pixels.
The image shows a routing concept, not the current application interface.

## Prompt

Use case: ads-marketing / ui-mockup.
Create a new final Open Graph image for ppmlx, an open-source local model router. Wide landscape 1200 x 630 pixels, 1.905:1 composition. The philosophy is "a local router to rule all models"; communicate this visually, do not print that sentence.
Mature, current developer-tool art direction matching a soft dark website: matte charcoal #111214, panels #18191c, quiet fine borders #303136, off-white #e5e5e7, restrained lavender #c4b9e8 and teal #9cc9ba. Crisp product UI illustration with slight depth, large clear geometry, abundant negative space. No neon, no space scene, no dramatic glow, no gradients behind text, no stock AI brain, no mascot, no marketing badges.
Composition: three small application window tiles at the left (terminal code icon, chat bubble icon, editor icon), thin graceful right-angle connector paths converge on a much larger ppmlx router window in the center. The router is visibly on a local desktop computer: subtle broad laptop/desktop-base silhouette beneath central window, not physical networking hardware. From the central router several distinct paths fan out to three compact abstract provider/model tiles at the right inside a subtle cloud-shaped grouping. Below the router inside the same local-computer boundary is a compact local-model chip, connected directly to the router, with a tiny connected-node context graph beside it. Make all paths visually traceable, deliberate topology, no messy random spaghetti. Route window contains three beautifully aligned short rows, tiny status dots and modest segmented policy controls as visual UI marks with NO fake body text. One lavender route is highlighted, other paths muted; teal local-model connection. This is a conceptual illustration, not a screenshot with paragraphs.
Only words: "ppmlx" prominently on central router, "LOCAL ROUTER" in small mono below it, "APPS" above left group, "MODELS" above cloud group, "LOCAL" below the local chip. Exact spelling ppmlx with lowercase l. No other text, no numbers, no provider logos.
Illustration fills about 80 percent of the frame, balanced 64px safe margins; key brand and routing structure remain legible at 600 x 315 social preview size. Refined flat frontal UI drawing with soft shadows and subtly layered panels, not a generic infographic template. Finished production image, no outer browser frame, no watermark.
